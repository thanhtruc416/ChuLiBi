# coding: utf-8
# Requires-Python: >=3.9
# Universal requirements file — compatible with Python 3.9 → 3.13+

# --- GUI & Visualization ---
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<0.14.0
pillow>=10.0.0,<11.0.0
ttkbootstrap>=1.10.0,<1.11.0

# --- Data Handling & Math ---
numpy>=1.26.4
pandas>=2.2.0,<3.0.0
scipy>=1.12.0,<1.15.0
statsmodels>=0.14.2,<0.15.0

# --- Machine Learning & Statistics ---
scikit-learn>=1.4.0,<1.6.0
xgboost>=2.0.0,<3.0.0
imbalanced-learn>=0.12.0,<0.13.0
joblib>=1.3.0,<2.0.0

# SHAP build notice:
# Nếu bạn đang ở Python 3.13 hoặc Windows chưa có MSVC compiler:
#   1. Tải và cài Microsoft Visual C++ Build Tools tại:
#      https://visualstudio.microsoft.com/visual-cpp-build-tools/
#   2. Tick “Desktop development with C++”
#   3. Sau khi cài, chạy lại lệnh:
#      pip install git+https://github.com/shap/shap.git
# Hoặc nếu chỉ đang test nhanh, bạn có thể tạm comment dòng shap bên dưới.
shap>=0.44.0,<0.46.0 ; python_version < "3.13"
git+https://github.com/shap/shap.git ; python_version >= "3.13"

# --- Database & Backend ---
pymysql>=1.1.0,<2.0.0
mysql-connector-python>=8.4.0,<10.0.0

# --- Email, Security & Environment ---
bcrypt>=4.1.0,<5.0.0
cryptography>=42.0.0,<45.0.0
email-validator>=2.1.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
requests>=2.31.0,<3.0.0

# --- Reporting & File Export ---
openpyxl>=3.1.2,<4.0.0
reportlab>=4.0.0,<5.0.0
pypandoc>=1.12,<2.0.0

# --- Optional fonts management for Matplotlib ---
fonttools>=4.50.0,<5.0.0

# --- Developer Tools ---
pytest>=8.0.0,<9.0.0

# --- Logging & Progress (optional but recommended) ---
tqdm>=4.66.0,<5.0.0
loguru>=0.7.0,<1.0.0

# --- Install guide ---
# 1. Upgrade pip first:
#     python -m pip install --upgrade pip setuptools wheel
# 2. Install all packages:
#     pip install -r requirements.txt
# 3. On Python 3.13, shap will be installed directly from GitHub
